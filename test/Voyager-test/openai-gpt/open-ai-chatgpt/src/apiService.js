/*const axios = require("axios");

const HTTP = "https://localhost:3001";

const apiService = {
  async chat(prompt) {
    try {
      const response = await axios.post(`${HTTP}/chat`, { prompt });
      return response.data;
    } catch (error) {
      console.log(error);
      return null;
    }
  },
};

module.exports = apiService;
*/